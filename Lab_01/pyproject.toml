[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "bank-system"
version = "0.1.0"
description = "Система обліку банківських рахунків"
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "Остап Зінчук" }
]

[project.scripts]
bank-system = "bank_system.main:main"